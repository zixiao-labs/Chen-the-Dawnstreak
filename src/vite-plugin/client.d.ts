declare module 'virtual:chen-routes' {
  import type { ComponentType } from 'react';
  /** Drop-in replacement for manual <Routes> setup. Place inside <ChenRouter>. */
  export const ChenRoutes: ComponentType;
}
